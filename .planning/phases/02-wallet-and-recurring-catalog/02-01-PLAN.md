---
phase: 02-wallet-and-recurring-catalog
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/**
  - packages/db/**
requirements_addressed:
  - CUST-01
  - CUST-02
  - CUST-03
  - CUST-04
must_haves:
  truths:
    - Customers are tenant-scoped and can be created, updated, searched, and statused without cross-tenant leakage.
    - Consent history is append-only and exposes the latest per-channel state.
    - Phone normalization and duplicate detection are deterministic at the repository boundary.
  artifacts:
    - packages/domain/src/wallet/**
    - packages/db/src/schema/wallet.ts
    - packages/db/src/repositories/wallet-repository.ts
    - packages/db/src/migrations/0003_wallet_foundation.sql
    - packages/db/test/wallet-repository.test.ts
---

# Phase 2 Plan 01: Modulo de clientes, validacao de telefone e consentimento

## Objective
- **What:** Add the wallet domain contracts and database foundation for customers plus consent tracking.
- **Why:** Phase 2 cannot proceed without a tenant-safe source of truth for who can be billed and contacted.
- **Output:** Domain schemas, SQL migration, repository methods, and repository tests for customers and consent history.

## Tasks

<task type="domain">
  <name>Model customer and consent contracts</name>
  <files>packages/domain/src/wallet/**; packages/domain/src/index.ts</files>
  <action>Add enums, input schemas, filters, and normalization helpers for customers and channel consent events.</action>
  <verify>Domain schemas accept valid Brazilian operational payloads and reject missing tenant/business requirements.</verify>
</task>

<task type="data">
  <name>Create wallet tables and repository methods</name>
  <files>packages/db/src/schema/wallet.ts; packages/db/src/schema/index.ts; packages/db/src/repositories/wallet-repository.ts; packages/db/src/repositories/app-repository.ts; packages/db/src/migrations/0003_wallet_foundation.sql</files>
  <action>Create tenant-scoped tables for customers and consent events, wire them into the shared repository, and expose methods for CRUD, consent append, duplicate detection, and list queries.</action>
  <verify>Repository methods never read or write wallet records without `tenant_id` scoping.</verify>
</task>

<task type="test">
  <name>Prove duplicate detection and consent derivation</name>
  <files>packages/db/test/wallet-repository.test.ts; packages/db/src/testing/test-auth-database.ts</files>
  <action>Write repository tests for duplicate phone blocking, consent append-only history, latest-consent projection, and tenant isolation.</action>
  <verify>`pnpm --filter @cobrazap/db test` covers wallet repository behaviors before API/UI work starts.</verify>
</task>
