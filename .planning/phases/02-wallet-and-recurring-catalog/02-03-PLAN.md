---
phase: 02-wallet-and-recurring-catalog
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - apps/web/**
requirements_addressed:
  - CUST-01
  - CUST-04
  - PLAN-01
  - PLAN-02
  - SUBS-01
  - SUBS-02
must_haves:
  truths:
    - Operators can manage customers, plans, and subscriptions in one authenticated wallet surface.
    - The web app uses the existing same-origin proxy pattern for all wallet mutations.
    - Empty, success, duplicate-warning, and status states are visible enough for operational onboarding.
  artifacts:
    - apps/web/src/app/(app)/painel/carteira/page.tsx
    - apps/web/src/components/wallet/**
    - apps/web/src/app/api/wallet/**
---

# Phase 2 Plan 03: Fluxos de onboarding operacional e telas de gestao da carteira

## Objective
- **What:** Deliver the operator-facing wallet screen and Next route handlers for the Phase 2 APIs.
- **Why:** The milestone needs a usable operational flow, not only raw data primitives.
- **Output:** `/painel/carteira` route, wallet components, and BFF proxies for the new API surface.

## Tasks

<task type="web">
  <name>Add same-origin route handlers for wallet endpoints</name>
  <files>apps/web/src/app/api/wallet/**; apps/web/src/lib/backend-proxy.ts</files>
  <action>Create route handlers mirroring the wallet API endpoints so the authenticated web app can operate entirely same-origin.</action>
  <verify>Every wallet fetch in the web layer goes through a local route handler.</verify>
</task>

<task type="web">
  <name>Build the wallet management screen</name>
  <files>apps/web/src/app/(app)/painel/carteira/page.tsx; apps/web/src/components/wallet/**; apps/web/src/components/settings/settings-screen.tsx</files>
  <action>Create a unified carteira screen with customer, plan, and subscription sections, inline forms, list states, duplicate warnings, and lifecycle actions.</action>
  <verify>The operator can complete the Phase 2 happy path from the browser shell without touching raw API tools.</verify>
</task>

<task type="quality">
  <name>Wire navigation and phase-level smoke validation</name>
  <files>apps/web/src/components/**; apps/web/package.json</files>
  <action>Add navigation entry points and make sure build/typecheck/test cover the new wallet screen and route handlers.</action>
  <verify>`pnpm --filter @cobrazap/web build` passes with the new route surface and authenticated UI.</verify>
</task>
